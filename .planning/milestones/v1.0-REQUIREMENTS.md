# Requirements: MCP Hangar

**Defined:** 2026-03-08
**Core Value:** Reliable, observable MCP provider management with production-grade lifecycle control -- providers start, run, degrade, and recover predictably with full audit trail.

## v1.0 Requirements

Requirements for v1.0 Production Hardening. Each maps to roadmap phases.

### Concurrency & Safety

- [x] **CONC-01**: Provider lock hierarchy is enforced -- `ProviderGroup._try_start_member()` releases group lock before acquiring provider lock, eliminating the level-11-holds-level-10 deadlock risk
- [x] **CONC-02**: Provider `_start()` releases its lock before performing I/O (subprocess launch, tool discovery), using INITIALIZING state guard and threading.Event for concurrent waiters
- [x] **CONC-03**: Provider `_refresh_tools()` performs tool discovery I/O outside lock scope, restructured to separate lock-acquire/release cycles within `invoke_tool()` control flow
- [x] **CONC-04**: StdioClient registers PendingRequest before writing request to stdin, preventing request-response mismatch under concurrent load

### Exception Hygiene

- [x] **EXCP-01**: All 42 bare `except Exception:` catches are categorized (fault-barrier, cleanup, bug-hiding) and each category is handled appropriately -- fault-barriers kept with structured logging, cleanup paths narrowed to specific exceptions, bug-hiding catches removed or replaced with specific error handling

### Security

- [x] **SECR-01**: Discovery-sourced provider commands (Docker labels, K8s annotations) are validated against an allowlist via `InputValidator.validate_command()` before provider registration
- [x] **SECR-02**: Rate limiting is enforced at command bus middleware layer, covering all entry points (stdio, HTTP, programmatic) regardless of transport

### State Persistence

- [x] **PERS-01**: Saga state is checkpointed to SQLite after each step transition, with schema managed by existing `MigrationRunner`
- [x] **PERS-02**: Incomplete sagas are detected and resumed on bootstrap, with idempotency guards preventing duplicate command emission during event replay
- [x] **PERS-03**: Circuit breaker state (state, failure_count, opened_at) persists across restarts via provider snapshots, preventing cascading failures against known-bad providers
- [x] **PERS-04**: Event store supports snapshots -- `IEventStore` contract includes snapshot methods, `SQLiteEventStore` implements snapshot storage and retrieval
- [x] **PERS-05**: Aggregate replay loads from latest snapshot plus subsequent events, bounding startup time regardless of total event history

### Resilience

- [x] **RESL-01**: Health check backoff uses exponential formula with jitter, preventing thundering herd on multi-provider recovery
- [x] **RESL-02**: `BackgroundWorker` schedules health checks based on provider state -- normal interval for READY, backoff with jitter for DEGRADED, longer ceiling for DEAD, skip for COLD
- [x] **RESL-03**: Docker discovery source reconnects automatically with retry and exponential backoff when the Docker daemon connection is lost

### Testing & Code Quality

- [x] **TEST-01**: Property-based tests using Hypothesis `RuleBasedStateMachine` verify that all Provider state transition sequences maintain invariants (valid transitions succeed, invalid transitions raise, events emitted, replay reproduces state)
- [x] **QUAL-01**: Package includes `py.typed` marker file and mypy strictness is incrementally enabled (`check_untyped_defs`, `no_implicit_optional`, `disallow_incomplete_defs`) with all resulting type errors fixed

## v2 Requirements

Deferred to future release. Tracked but not in current roadmap.

### Exception Hygiene

- **EXCP-02**: Ruff BLE001 bare-except lint rule enabled project-wide to prevent regressions after audit completion

### State Persistence

- **PERS-06**: Saga compensation automatically undoes completed steps on partial failure (requires PERS-01 saga persistence)
- **PERS-07**: Circuit breaker supports HALF_OPEN state allowing controlled probe requests through tripped breaker
- **PERS-08**: Snapshot compaction prunes old events after snapshot creation, bounding storage growth

### Resilience & Observability

- **RESL-04**: Prometheus metrics emitted for rate limit hits, rejections, and bucket utilization

### Testing

- **TEST-02**: Fuzz testing for event deserialization validates resilience to malformed or corrupted event data

## Out of Scope

Explicitly excluded. Documented to prevent scope creep.

| Feature | Reason |
|---------|--------|
| Distributed saga coordination (cross-node) | Single-process by design. Distributed sagas add massive complexity for zero benefit in current architecture. |
| External circuit breaker service (Resilience4j, sidecar) | Over-engineering. Adds operational dependency for a simple state machine. |
| Real-time event streaming / event bus replication | Not needed for single-process hardening. Streaming is a scale-out concern. |
| ML-based anomaly detection for health checks | Deterministic backoff with jitter covers 99% of cases. ML adds unpredictability without proportional value. |
| Per-request command allowlist UI / admin panel | Scope creep. Allowlist should be configuration-driven (YAML), not require a separate UI. |
| Async/asyncio rewrite of domain layer | CLAUDE.md explicitly states thread-based by design. Domain layer works. |
| Custom binary snapshot serialization format | JSON via existing `to_dict()`/`from_dict()` is sufficient. Optimize only if profiling shows bottleneck. |

## Traceability

Which phases cover which requirements. Updated during roadmap creation.

| Requirement | Phase | Status |
|-------------|-------|--------|
| CONC-01 | Phase 8 | Complete |
| CONC-02 | Phase 8 | Complete |
| CONC-03 | Phase 8 | Complete |
| CONC-04 | Phase 8 | Complete |
| EXCP-01 | Phase 8 | Complete |
| SECR-01 | Phase 8 | Complete |
| SECR-02 | Phase 10 | Complete |
| PERS-01 | Phase 9 | Complete |
| PERS-02 | Phase 9 | Complete |
| PERS-03 | Phase 9 | Complete |
| PERS-04 | Phase 10 | Complete |
| PERS-05 | Phase 10 | Complete |
| RESL-01 | Phase 10 | Complete |
| RESL-02 | Phase 10 | Complete |
| RESL-03 | Phase 10 | Complete |
| TEST-01 | Phase 10 | Complete |
| QUAL-01 | Phase 10 | Complete |

**Coverage:**

- v1.0 requirements: 17 total
- Mapped to phases: 17
- Unmapped: 0

---
*Requirements defined: 2026-03-08*
*Last updated: 2026-03-08 -- v1.0 milestone complete, all 17 requirements satisfied*
