# Milestone v7.0: Kubernetes Enforcement Foundation + Licensing

**Status:** PLANNED
**Target:** 2026-04-15
**PyPI release:** v0.13.0
**Phases:** 35-41
**Tracks:** Core (Python), Operator (Go), Helm Charts
**Coordination:** Licensing phases (35-37) are a hard pre-requisite for all other phases and for starting any BSL-licensed work. K8s phases (38-41) can start in parallel once Phase 35 is complete.

## Overview

Two parallel tracks with one strict dependency:

**Licensing track (35-37):** Migrate Pro/Enterprise features from `src/` to `enterprise/` under BSL 1.1, establish the import boundary CI check, and add the license file. This is a structural pre-requisite -- no new enterprise features can be developed until the boundary exists. See `.planning/MIGRATION_ENTERPRISE.md` for the detailed migration table.

**K8s enforcement track (38-41):** Implement the capability declaration schema, NetworkPolicy generation from capabilities in the operator, the enforcement loop (policy reconciliation + violation signals), and admission/policy integration. This makes Hangar the most secure way to run MCP servers in Kubernetes.

MIT licensed work goes in `src/` and `packages/operator/`. BSL licensed work goes in `enterprise/`.

## Scope

Source: ROADMAP.md -- "Phase 1: Kubernetes Enforcement Foundation (April -- May)"
Source: PRODUCT_ARCHITECTURE.md -- "Licensing Model" and "Hardening Priorities (Critical)"

### Licensing & Structure

| Item | Priority |
|------|----------|
| Extract interfaces/contracts for enterprise features to `domain/contracts/` and `application/ports/` | P0 |
| Create `enterprise/` directory structure per MIGRATION_ENTERPRISE.md | P0 |
| Move Pro/Enterprise implementations to `enterprise/` (auth, policies, persistence, langfuse, dashboard) | P0 |
| Add CI import boundary check: `src/` never imports from `enterprise/` | P0 |
| Add `enterprise/LICENSE.BSL` + update root `LICENSE` scope comment | P0 |

### Kubernetes Enforcement

| Item | Priority | Package(s) |
|------|----------|------------|
| Capability declaration schema (`capabilities` block in provider config + CRD) | P0 | core, operator |
| NetworkPolicy generation from capabilities (operator) | P0 | operator |
| Operator enforcement loop (capability enforcement + violation signaling) | P0 | operator |
| Admission and policy integration (reject unsafe specs before runtime) | P0 | operator |
| Runtime capability verification (declared vs observed) | P0 | core |
| Violation signals (denied egress, capability drift, policy rejection, quarantine) | P0 | core, operator |
| Container filesystem sandboxing (read-only root default, explicit mount allowlist) | P1 | core, operator |
| Seccomp / AppArmor profiles for MCP server containers | P1 | operator, helm-charts |
| Image provenance verification (cosign/notation, strict mode) | P2 | core, operator |

### Hardening (critical, same release)

| Item | Priority |
|------|----------|
| CI security scanning: Trivy/Grype on images, Semgrep on source | P0 |
| Dependency audit: `pip-audit`, `npm audit`, SBOM generation | P0 |
| Auth stack test coverage audit (target 90%+) | P0 |

## Existing Foundation

| Area | What exists | What v7.0 adds |
|------|-------------|----------------|
| Provider config | `mode`, `command`, `env`, `idle_ttl_s`, `health_check` | `capabilities` block: egress destinations, filesystem paths, env var declarations, expected tool schemas |
| K8s operator | MCPProvider/MCPProviderGroup/MCPDiscoverySource CRDs, state-machine reconciliation | NetworkPolicy generation, capability enforcement loop, violation signals, admission webhook |
| Auth/RBAC | In `src/mcp_hangar/infrastructure/auth/` (MIT, exposed) | Moved to `enterprise/auth/` (BSL); core defines `IAuthProvider` contract only |
| Event sourcing | SQLite/Postgres stores in `src/infrastructure/persistence/` | Moved to `enterprise/persistence/`; core keeps in-memory store (MIT) |
| CI | Tests, linting, type checking | + Trivy/Grype, Semgrep, pip-audit, import boundary check |

## Phases

### Phase 35: Extract Enterprise Contracts (Licensing pre-req 1/3) -- COMPLETE (2026-03-24)

**Goal:** Verify and fill in all interface contracts that enterprise implementations depend on, so that enterprise code can compile independently from core implementations.
**Depends on:** v5.0 complete
**Target:** v0.13.0
**License:** MIT (contracts live in core)
**Plans:** 3 plans

Plans:
- [x] 35-01-PLAN.md -- Define 2 missing contracts: `IToolAccessPolicyEnforcer` (authorization.py) + `IDurableEventStore` (event_store.py). Update `__init__.py` re-exports. (4 existing contracts already cover IAuthenticator, IAuthorizer/IRoleStore/IApiKeyStore, IEventStore, ObservabilityPort/IAuditExporter.)
- [x] 35-02-PLAN.md -- Create Null implementations for all enterprise contracts: `NullAuthenticator`, `NullApiKeyStore`, `NullAuthorizer`, `NullRoleStore`, `NullToolAccessPolicyStore`, `NullToolAccessPolicyEnforcer`. Update `__init__.py` re-exports.
- [x] 35-03-PLAN.md -- Unit tests: verify each contract has a working Null implementation (isinstance checks, functional correctness). Validate IDurableEventStore extends IEventStore. Complete inventory check.

**Success Criteria:**
1. All 6 enterprise contracts defined in `domain/contracts/` or `application/ports/`.
2. Each contract has a `Null*` or in-memory implementation in core usable in tests without enterprise code.
3. Existing concrete implementations in `src/` compile against the new interfaces (no breaking changes yet).

---

### Phase 36: Enterprise Directory Migration (Licensing pre-req 2/3) -- COMPLETE (2026-03-24)

**Goal:** Move Pro/Enterprise implementations from `src/` to `enterprise/` per MIGRATION_ENTERPRISE.md. Update all internal imports.
**Depends on:** Phase 35
**Target:** v0.13.0
**License:** BSL (enterprise/); MIT (core adapters/null impls)
**Plans:** 4 plans

Plans:
- [x] 36-01-PLAN.md -- Verify enterprise/ directory structure completeness and LICENSE.BSL. Polish placeholder __init__.py docstrings.
- [x] 36-02-PLAN.md -- Verify auth migration: audit all auth shims in src/ and bootstrap conditional loading points.
- [x] 36-03-PLAN.md -- Verify persistence/observability migration: audit SQLiteEventStore and Langfuse shims and bootstrap fallbacks.
- [x] 36-04-PLAN.md -- Write bootstrap enterprise boundary tests proving server works with and without enterprise modules.

**Success Criteria:**
1. `enterprise/` directory exists with correct structure and `enterprise/LICENSE.BSL`.
2. All enterprise implementations moved. Core no longer contains BSL-licensed code.
3. Bootstrap conditionally loads enterprise modules. Server starts without enterprise modules using null implementations.
4. All tests pass (unit + integration).

---

### Phase 37: CI Import Boundary + License Verification (Licensing pre-req 3/3) -- COMPLETE (2026-03-24)

**Goal:** Enforce the import boundary in CI and update root LICENSE scope comment. Finalize licensing setup for v0.13.0 tag.
**Depends on:** Phase 36
**Target:** v0.13.0
**Plans:** 2 plans

Plans:
- [x] 37-01-PLAN.md -- CI check: `grep -rn "from enterprise\|import enterprise" src/ packages/operator/ packages/helm-charts/` must return empty. Add to `.github/workflows/` as a separate lint step on every PR.
- [x] 37-02-PLAN.md -- Update root `LICENSE` header comment to clarify MIT scope (everything outside `enterprise/`). Update `README.md` licensing section. Update `pyproject.toml` classifiers. Add `CLA.md` acknowledgment to `CONTRIBUTING.md` for enterprise/ contributions.

**Success Criteria:**
1. CI import boundary check passes (no `from enterprise` in `src/` or `packages/`).
2. Root `LICENSE` clearly scopes MIT to everything outside `enterprise/`.
3. `CONTRIBUTING.md` explains CLA requirement for enterprise contributions.
4. CI is green.

---

### Phase 38: Capability Declaration Schema (K8s enforcement 1/4) -- COMPLETE (2026-03-24)

**Goal:** Extend provider config with a `capabilities` block that declares what resources an MCP server needs. Machine-readable, auditable, consumed by the operator for policy generation.
**Depends on:** Phase 35 (contracts must exist before core changes)
**Target:** v0.13.0
**License:** MIT (core)
**Plans:** 3 plans

Plans:
- [x] 38-01-PLAN.md -- Add from_dict() factory to ProviderCapabilities, wire capabilities into Provider aggregate, ProviderConfig dataclass, and server/config.py YAML loading (Wave 1)
- [x] 38-02-PLAN.md -- Extend MCPProvider CRD with ProviderCapabilities Go structs, kubebuilder validation markers, reconciler status propagation (Wave 1, parallel with 01)
- [x] 38-03-PLAN.md -- from_dict() round-trip tests, config loading integration tests, ConfigurationError boundary tests, update example configs with capabilities blocks (Wave 2, depends on 01+02)

**Success Criteria:**
1. `ProviderCapabilities` value object is immutable, self-validating (invalid egress patterns raise `ConfigurationError`).
2. Provider config YAML accepts a `capabilities` block; missing capabilities defaults to unconstrained (not an error, but logged as warning).
3. CRD `spec.capabilities` field accepted and stored in operator status.
4. Example configs updated with capabilities examples.

---

### Phase 39: NetworkPolicy Generation (K8s enforcement 2/4)

**Goal:** Operator auto-generates Kubernetes NetworkPolicy resources from `spec.capabilities.egress` declarations. Undeclared outbound connections are blocked and logged.
**Depends on:** Phase 38
**Target:** v0.13.0
**License:** MIT (operator)
**Plans:** 4 plans

Plans:
- [ ] 39-01-PLAN.md -- `NetworkPolicyReconciler` in operator: for each `MCPProvider` with capabilities, generate a `NetworkPolicy` in the same namespace. Egress rules derived from `spec.capabilities.egress`. Default-deny all other egress.
- [ ] 39-02-PLAN.md -- `NetworkPolicyBuilder` (Go): translate `ProviderCapabilities.egress` (host:port patterns) to Kubernetes `NetworkPolicyEgressRule` structs. Handle DNS resolution policy, port wildcards, and CIDR blocks.
- [ ] 39-03-PLAN.md -- Docker provider equivalent: generate Docker network rules (bridge with explicit egress) when `capabilities.egress` is declared in Docker mode. Fallback for non-K8s environments.
- [ ] 39-04-PLAN.md -- Operator tests: reconciler creates/updates/deletes `NetworkPolicy` in response to `MCPProvider` create/update/delete. Verify default-deny egress baseline.

**Success Criteria:**
1. `MCPProvider` with `spec.capabilities.egress: [{host: "api.example.com", port: 443}]` produces a `NetworkPolicy` with matching egress rule and default-deny.
2. Deleting `MCPProvider` deletes the `NetworkPolicy`.
3. Updating capabilities updates the `NetworkPolicy` in the same reconciliation loop.
4. Docker mode: provider container started with network rules from capabilities.

---

### Phase 40: Operator Enforcement Loop + Violation Signals (K8s enforcement 3/4)

**Goal:** Make the operator the primary engine for capability enforcement. Emit explicit violation signals for denied egress, capability drift, and quarantine actions. These signals are the first-class contract for downstream SIEM/alerting integration.
**Depends on:** Phase 39, Phase 31 (OTEL conventions from v6.0 P0)
**Target:** v0.13.0
**License:** MIT (core, operator)
**Plans:** 4 plans

Plans:
- [ ] 40-01-PLAN.md -- Domain events: `CapabilityViolationDetected`, `EgressDenied`, `ProviderQuarantined` in `domain/events.py`. Value objects: `ViolationType` enum, `ViolationSeverity` enum.
- [ ] 40-02-PLAN.md -- Operator: when network activity to an undeclared destination is detected (via eBPF/CNI events or NetworkPolicy audit logs), emit a `CapabilityViolationDetected` event through the event bus. Record violation in provider status (`status.violations`).
- [ ] 40-03-PLAN.md -- Violation signals in OTLP (uses Phase 31 conventions): `mcp.violation.type`, `mcp.violation.severity`, `mcp.provider.id`, `mcp.enforcement.action`. Prometheus metric: `mcp_hangar_capability_violations_total{provider, violation_type}`.
- [ ] 40-04-PLAN.md -- Tests: violation event emission, OTLP span attributes, Prometheus metric increment, provider status update on violation.

**Success Criteria:**
1. `CapabilityViolationDetected` event emitted when undeclared egress is attempted.
2. Violation spans exported through OTLP with governance attributes.
3. `mcp_hangar_capability_violations_total` counter increments on each violation.
4. `MCPProvider` status shows `status.violations` list with timestamp and violation type.

---

### Phase 41: Admission + Runtime Capability Verification (K8s enforcement 4/4)

**Goal:** Reject unsafe provider specs before runtime via admission/policy integration. Compare declared capabilities against observed behavior at runtime and alert on deviation.
**Depends on:** Phase 40
**Target:** v0.13.0
**License:** MIT (core, operator)
**Plans:** 4 plans

Plans:
- [ ] 41-01-PLAN.md -- Validating admission webhook in operator: reject `MCPProvider` specs with invalid or unsafe capability declarations (e.g., `egress: ["*:*"]` without explicit override annotation, missing required fields). CEL validation rules in CRD as first-pass filter.
- [ ] 41-02-PLAN.md -- Runtime capability verifier in core: after provider reaches READY, compare actual discovered tool schemas against `capabilities.expected_tools`. Emit `CapabilityViolationDetected` with `type: SCHEMA_DRIFT` on mismatch.
- [ ] 41-03-PLAN.md -- Optional hard-block mode: `capabilities.enforcement_mode: strict` -- provider transitions to DEAD on capability violation instead of just alerting. Default: `alert`.
- [ ] 41-04-PLAN.md -- Tests: admission webhook rejects unsafe spec; runtime verifier detects schema drift; strict mode causes DEAD transition; alert mode logs + emits event only.

**Success Criteria:**
1. Admission webhook rejects `MCPProvider` with `egress: ["*:*"]` (wildcard egress) unless `hangar.io/allow-unrestricted-egress: "true"` annotation is present.
2. Provider with declared `expected_tools: [{"name": "add", "schema_hash": "abc123"}]` emits `SCHEMA_DRIFT` violation if the actual tool schema differs.
3. `enforcement_mode: strict` causes DEAD state transition on first violation.
4. `enforcement_mode: alert` (default) emits event and metric without stopping the provider.

## Phase Dependency Graph

```
Licensing Track:    35 (Contracts) --> 36 (Migration) --> 37 (CI + License)
                           |
                           v (unblocks K8s track start)
K8s Track:          38 (Capabilities) --> 39 (NetworkPolicy) --> 40 (Enforcement + Signals) --> 41 (Admission + Verification)

v0.13.0 gate: Phases 35-41 complete (all P0), CI green, import boundary check passing
```

## Dependencies

- Phase 31 (v6.0) must be complete before Phase 40 (violation signal attributes)
- MIGRATION_ENTERPRISE.md is the source of truth for Phase 36 file moves
- Operator work (Phases 38-41) assumes Go 1.23 + controller-runtime

---

*Milestone v7.0 planned 2026-03-24.*
