---
phase: 14-dashboard-provider-management
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/components/ui/ProviderStateBadge.tsx
  - packages/ui/src/components/ui/HealthBadge.tsx
  - packages/ui/src/components/ui/CircuitBreakerBadge.tsx
  - packages/ui/src/components/ui/MetricCard.tsx
  - packages/ui/src/components/ui/ActionButton.tsx
  - packages/ui/src/components/ui/EmptyState.tsx
  - packages/ui/src/components/ui/LoadingSpinner.tsx
  - packages/ui/src/components/ui/index.ts
autonomous: true
requirements:
  - UI-14
must_haves:
  truths:
    - "ProviderStateBadge renders colour-coded pill for each of the 5 provider states"
    - "HealthBadge renders indicator for healthy/degraded/unhealthy/unknown"
    - "CircuitBreakerBadge renders closed/open/half_open indicator"
    - "MetricCard shows a labelled number with optional sub-label"
    - "ActionButton renders with loading and disabled states"
    - "EmptyState and LoadingSpinner are available for table/list pages"
    - "All components exported from a barrel index with zero TypeScript errors"
  artifacts:
    - path: "packages/ui/src/components/ui/ProviderStateBadge.tsx"
      provides: "Colour-coded badge for ProviderState enum"
      exports: ["ProviderStateBadge"]
    - path: "packages/ui/src/components/ui/HealthBadge.tsx"
      provides: "Badge for HealthStatus.status"
      exports: ["HealthBadge"]
    - path: "packages/ui/src/components/ui/CircuitBreakerBadge.tsx"
      provides: "Badge for CircuitBreakerStatus.state"
      exports: ["CircuitBreakerBadge"]
    - path: "packages/ui/src/components/ui/MetricCard.tsx"
      provides: "Card with label + value + optional sub-label"
      exports: ["MetricCard"]
    - path: "packages/ui/src/components/ui/ActionButton.tsx"
      provides: "Button with isLoading + disabled support"
      exports: ["ActionButton"]
    - path: "packages/ui/src/components/ui/EmptyState.tsx"
      provides: "Centred empty-state block with optional message"
      exports: ["EmptyState"]
    - path: "packages/ui/src/components/ui/LoadingSpinner.tsx"
      provides: "Animated spinner for loading states"
      exports: ["LoadingSpinner"]
    - path: "packages/ui/src/components/ui/index.ts"
      provides: "Barrel re-export of all primitives"
      contains: "export"
  key_links:
    - from: "packages/ui/src/components/ui/index.ts"
      to: "all feature pages"
      via: "import { ProviderStateBadge, ... } from '../../components/ui'"
      pattern: "export.*from './"
---

<objective>
Build the shared UI primitive components that all Phase 14 feature pages depend on.

Purpose: All dashboard/provider/group/discovery/config pages need the same state badges, metric cards, and action buttons. Building them once as typed components with consistent Tailwind styling prevents duplication and gives the Wave 2 plans stable contracts to build against.

Output: 7 component files + 1 barrel index in `packages/ui/src/components/ui/`. Zero TypeScript errors. Ready for import by Wave 2 plans.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/ui/src/types/provider.ts
@packages/ui/src/types/system.ts
@packages/ui/src/lib/cn.ts
</context>

<interfaces>
<!-- Key types from packages/ui/src/types/provider.ts -->
```typescript
export type ProviderState = 'cold' | 'initializing' | 'ready' | 'degraded' | 'dead'

export interface HealthStatus {
  status: 'healthy' | 'degraded' | 'unhealthy' | 'unknown'
  last_check?: string
  consecutive_failures: number
  message?: string
}

export interface CircuitBreakerStatus {
  state: 'closed' | 'open' | 'half_open'
  failure_count: number
  opened_at?: string
}

```

<!-- From packages/ui/src/lib/cn.ts -->
```typescript
export function cn(...inputs: ClassValue[]): string  // clsx + tailwind-merge
```

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ProviderStateBadge, HealthBadge, CircuitBreakerBadge</name>
  <files>
    packages/ui/src/components/ui/ProviderStateBadge.tsx,
    packages/ui/src/components/ui/HealthBadge.tsx,
    packages/ui/src/components/ui/CircuitBreakerBadge.tsx
  </files>
  <action>
Create `packages/ui/src/components/ui/` directory and three badge components using Tailwind classes (no @radix-ui/react-badge — that package does not exist).

**ProviderStateBadge.tsx**
Props: `{ state: ProviderState; className?: string }`
Render a `<span>` pill with `text-xs font-medium px-2 py-0.5 rounded-full` and state-specific colour classes:

- `cold`: `bg-gray-100 text-gray-600`
- `initializing`: `bg-blue-100 text-blue-700`
- `ready`: `bg-green-100 text-green-700`
- `degraded`: `bg-yellow-100 text-yellow-700`
- `dead`: `bg-red-100 text-red-700`
Display the state string capitalised (e.g. "Cold", "Ready").

**HealthBadge.tsx**
Props: `{ status: HealthStatus['status']; className?: string }`
Same pill styling:

- `healthy`: `bg-green-100 text-green-700` + "Healthy"
- `degraded`: `bg-yellow-100 text-yellow-700` + "Degraded"
- `unhealthy`: `bg-red-100 text-red-700` + "Unhealthy"
- `unknown`: `bg-gray-100 text-gray-500` + "Unknown"

**CircuitBreakerBadge.tsx**
Props: `{ state: CircuitBreakerStatus['state']; className?: string }`

- `closed`: `bg-green-100 text-green-700` + "Closed"
- `open`: `bg-red-100 text-red-700` + "Open"
- `half_open`: `bg-yellow-100 text-yellow-700` + "Half-open"

All three import `cn` from `../../lib/cn`. Use the `ProviderState`, `HealthStatus`, `CircuitBreakerStatus` types from `../../types/provider`.
  </action>
  <verify>
    <automated>cd packages/ui && npm run type-check 2>&1 | tail -5</automated>
  </verify>
  <done>Three badge files exist, import cleanly, type-check passes with zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: MetricCard, ActionButton, EmptyState, LoadingSpinner + barrel index</name>
  <files>
    packages/ui/src/components/ui/MetricCard.tsx,
    packages/ui/src/components/ui/ActionButton.tsx,
    packages/ui/src/components/ui/EmptyState.tsx,
    packages/ui/src/components/ui/LoadingSpinner.tsx,
    packages/ui/src/components/ui/index.ts
  </files>
  <action>
Create four utility components and a barrel index.

**MetricCard.tsx**
Props: `{ label: string; value: string | number; subLabel?: string; className?: string }`
Render a `<div className="bg-white rounded-lg border border-gray-200 p-4">` card:

- `<p className="text-sm text-gray-500">{label}</p>`
- `<p className="text-2xl font-bold text-gray-900 mt-1">{value}</p>`
- If `subLabel`: `<p className="text-xs text-gray-400 mt-0.5">{subLabel}</p>`

**ActionButton.tsx**
Props: `{ onClick: () => void; isLoading?: boolean; disabled?: boolean; variant?: 'primary' | 'danger' | 'ghost'; children: React.ReactNode; className?: string }`
Base classes: `inline-flex items-center gap-1.5 px-3 py-1.5 text-sm font-medium rounded-md transition-colors disabled:opacity-50 disabled:cursor-not-allowed`
Variant classes:

- `primary` (default): `bg-blue-600 text-white hover:bg-blue-700`
- `danger`: `bg-red-600 text-white hover:bg-red-700`
- `ghost`: `bg-transparent text-gray-700 border border-gray-300 hover:bg-gray-50`
When `isLoading=true`: show a 12px `<LoadingSpinner />` inline + disable the button.

**EmptyState.tsx**
Props: `{ message?: string; className?: string }`
Render centred flex column: icon (a simple SVG circle-dashed or an inline lucide `Inbox` icon at 32px) + `<p className="text-sm text-gray-400 mt-2">{message ?? 'No items found.'}</p>`. Full div: `flex flex-col items-center justify-center py-12 text-center`.

**LoadingSpinner.tsx**
Props: `{ size?: number; className?: string }` (default size 16)
Render a `<svg>` with an animating circle using `animate-spin` Tailwind class. Use an SVG circle with `stroke-dasharray="60" stroke-dashoffset="20"` for the partial arc.

```tsx
<svg
  width={size}
  height={size}
  viewBox="0 0 24 24"
  fill="none"
  className={cn('animate-spin text-current', className)}
>
  <circle cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="3" strokeOpacity="0.2" />
  <path d="M12 2a10 10 0 0 1 10 10" stroke="currentColor" strokeWidth="3" strokeLinecap="round" />
</svg>
```

**index.ts** — barrel re-export:

```typescript
export { ProviderStateBadge } from './ProviderStateBadge'
export { HealthBadge } from './HealthBadge'
export { CircuitBreakerBadge } from './CircuitBreakerBadge'
export { MetricCard } from './MetricCard'
export { ActionButton } from './ActionButton'
export { EmptyState } from './EmptyState'
export { LoadingSpinner } from './LoadingSpinner'
```

  </action>
  <verify>
    <automated>cd packages/ui && npm run type-check 2>&1 | tail -5</automated>
  </verify>
  <done>All 4 utility components + index.ts exist, type-check passes with zero errors, all 7 components exported from barrel index.</done>
</task>

</tasks>

<verification>
`cd packages/ui && npm run type-check` must exit 0.
All 8 files in `packages/ui/src/components/ui/` must exist (7 components + index.ts).
</verification>

<success_criteria>

- Zero TypeScript errors after both tasks
- 7 components + barrel index exist under `packages/ui/src/components/ui/`
- Each badge component handles all valid enum values with distinct colours
- ActionButton disables + shows spinner during isLoading
- No @radix-ui/react-badge import (package does not exist)
</success_criteria>

<output>
After completion, create `.planning/phases/14-dashboard-provider-management/14-01-SUMMARY.md` using the summary template at `@./.opencode/get-shit-done/templates/summary.md`.
</output>
