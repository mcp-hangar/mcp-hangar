# Unignore src/lib/ - the root .gitignore blocks lib/ but that is for Python venv lib/
!src/lib/
!src/lib/**

# Build output
dist/
dist-ssr/

# Local env files
.env.local
.env.*.local

# Dependencies (tracked via package-lock.json)
node_modules/
